# This crate is deprecated

The `whis` crate has been renamed to [`whis-cli`](https://crates.io/crates/whis-cli).

## Install the new package

```bash
cargo install whis-cli
```

The command remains `whis`.
