fn main() {
    eprintln!();
    eprintln!("  The 'whis' crate has been renamed to 'whis-cli'");
    eprintln!();
    eprintln!("  To install the latest version, run:");
    eprintln!();
    eprintln!("      cargo install whis-cli");
    eprintln!();
    eprintln!("  Then use the 'whis' command as before.");
    eprintln!();
    std::process::exit(1);
}
